generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Stage {
  To_Do
  In_Progress
  Done
}

model Tasks {
  id           String   @id
  title        String
  description  String
  stage        Stage
}